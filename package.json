{
  "name": "flow-drawer",
  "version": "0.0.1",
  "description": "Node-RED flow drawer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib",
    "start": "node --trace-warnings lib/index.js > index.html"
  },
  "author": "Artem Ustimov <artem.ustimov@extern.isban.com>",
  "license": "Apache 2.0",
  "dependencies": {
    "canvas": "^2.0.0-alpha.14",
    "d3": "^3.5.17",
    "jsdom": "^12.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
